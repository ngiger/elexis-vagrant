Demo from template 
First all keys
<% scope.to_hash.keys.each do |k| -%>
<%= k %>
<% end -%>


domain is <%= scope.lookupvar('::domain') %>
host is <%= scope.lookupvar('::hostname') %>
macaddress is <%= scope.lookupvar('::macaddress') %>
is_virtual is <%= scope.lookupvar('::is_virtual') %>
macaddress_eth0 is <%= scope.lookupvar('::macaddress_eth0') %>
macaddress is <%= scope.lookupvar('::macaddress') %>

# Dummy content with a variable
<% for @item in  @ntpservers %>
  server = <%= @item %>
<% end %>

# ntp info is <% @ntpinfo %>

# as array
 <%= @ntpinfo %>

<% if @ntpinfo %>
ntpinfo has <%= @ntpinfo %> value
<% else %>
ntpinfo is undefined
<% end %>
#end

