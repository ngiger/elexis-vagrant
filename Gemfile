source "http://rubygems.org"
# ruby '2.1.2'

if false
  # we now use vagrant installed from deb package
  gem "vagrant"
  gem "vagrant-docker"
  group :plugins do
    gem "vagrant-docker"
  end
end

gem 'puppet'
gem 'librarian-puppet'